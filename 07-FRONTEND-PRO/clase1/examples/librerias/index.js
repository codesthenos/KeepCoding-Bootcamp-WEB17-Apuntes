const array = ['a', 'b', 'c', 'd']
// lodash _.chunk return a new array para usar lodash _.
const array2 = _.chunk(array, 2)
console.log(array, array2)
// luxon
const date = luxon.DateTime.now().toString()
console.log(date)